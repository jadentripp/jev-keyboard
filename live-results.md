# Jev live keyboard results — September 22, 2026

Gateway access now succeeds. All 28 successful evaluation requests reported $0 cost
(total reported input tokens: 69,388). This confirms the free promotion
was applied to these requests; it does not establish unlimited request capacity.

The keyboard has 98 choices: all 94 non-space printable ASCII characters plus
SPACE, ENTER, BACKSPACE and DONE. DONE ends the generation loop. Each new request
receives the draft produced by the previous action. Both runs started empty.

| Run | Setup | Selected actions | Seconds | Result | Stop reason |
| --- | --- | ---: | ---: | --- | --- |
| 1 | Original prompt, literal character keys | 13 | 74.3 | `Q` followed by 12 spaces | Repetition guard |
| 2 | Explicit action descriptions and resulting draft for every key | 14 | 85.8 | `A Q `, interspersed with typing/deleting `a` | Repetition guard |

The second prompt asked for one next character, completion of unfinished words,
no heading, no space after a space, no punctuation before a complete phrase,
and DONE only after a complete question. Every option described its action and
showed exactly what the draft would become. It still failed to produce a sentence.

Six independent diagnostic questions were evaluated in one additional request:

| Probe | Selected output |
| --- | --- |
| Empty prefix, begin a question | `?` |
| Continue `Wh` without a supplied complete target | `a` |
| Continue `What is ` | Space |
| Continue `How can we distinguish intelligence from ` | `w` |
| Copy the next character after `Wh` from supplied target `What is wisdom?` | `a` |
| Choose a full-word continuation from five supplied words | `wisdom` |

These diagnostics include deliberately supplied prefixes/targets and candidate
words; they are not original text generated by Jev. Neither free-writing run
produced a coherent question or selected DONE. No assistant-written wording was
substituted for model output. The evidence shows that these two greedy character
prompts fail; it does not prove that every possible character-decoding strategy
will fail. Six earlier local tests tested loop behavior, not language quality.

Full model probability distributions, token usage, timing and cost metadata are
in the attached JSONL traces. The diagnostic JSON contains its full request and
response. Credentials are excluded.
